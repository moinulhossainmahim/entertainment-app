import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  iconButton: {
    position: "absolute",
    right: "8px",
    top: "8px",
    fontSize: "2rem",
    color: theme.palette.grey[500],
  },
}));
