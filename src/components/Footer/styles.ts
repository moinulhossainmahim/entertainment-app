import { makeStyles } from "@material-ui/styles";

export default makeStyles(() => ({
  footer: {
    position: "fixed",
    bottom: "0",
    width: "100%",
    backgroundColor: "#2E313A",
    height: "5rem",
  },
}));
