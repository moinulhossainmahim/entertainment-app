import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  sliderContainer: {
    padding: "1rem 3rem",
    backgroundColor: "#39445A",
  },
  sliderImg: {
    width: "100%",
    height: "50vh",
  },
}));
